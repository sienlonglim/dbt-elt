version: 2

sources:
  - name: raw
    description: ingested raw data
    schema: raw
    tables: 
      - name: raw__hdb_resale_records
        description: Resale flat transactions, one record per flat sold