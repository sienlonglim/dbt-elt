version: 2

seeds:
  - name: postcodes
    description: One record per unique address
    config:
      quote_columns: true
      delimiter: ','
    columns:
      - name: postcode
        description: 6 digit postcode
        tests:
          - not_null
      - name: coordinates
        description: tuple coordinates
        tests:
          - not_null